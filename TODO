TODOs
-----

* Start an erlang node per maven project, because mvn erlang:test-upload will not upload testable dependend modules
* switch the distel default node-name to the node of the project of the current buffer when switching buffers
(Does a buffer-switch-hook exists???)
