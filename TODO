TODOs
-----

* make erlang node buffer a full fledged erl shell

* load erlang modules of erl-mvn into every erlang node

* add eunit integration: run current test 

* switching between test and source

* assure the kill stays clean

* add current artifact to buffer status bar

* add erl-mvn-erlang-source minor mode that has:
  -- current buffer --
  * goto test
  * compile file
  * show compilation results
  -- current project --
  * goto pom
  * show erlang output
  * run maven build
  * show maven output
  -- other projects
  * goto source in project > [source files]
  * goto dependend source > [projects > [sources]]

* add erl-mvn-erlang-test-source minor mode that has:
  * goto source
  * run tests
  * run test under point

* major mode for poms:
  - shows artifact, group id and version
  - coupled with open/close project functions
  - runs maven
  - shell to erlang node
  - shows all sources and test sources
  - shows dependencies
  - provide a search local dependend project
  - add supervisors, gen_servers, etc... from templates, including unit tests
  - run all tests
  - show all tests
  
* minor mode for gen_* modules
  - create cast
  - create call

* extend project identifaction mechanism replacing the ubiquitous artifactid by a triple of groupid, artifactid, verion(GAV) 

* improve error handling where appropriate


